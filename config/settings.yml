plugins:
  shared_edits_enabled:
    default: true
    client: true
  shared_edits_editor_mode:
    default: "markdown"
    client: true
    type: enum
    choices:
      - markdown
      - rich
  shared_edits_commit_delay_seconds:
    default: 30
    min: 5
    max: 300
